# ============================================================================
# Hybrid-Node ApexFusion Testnet (AFPT) Relay for K3s
# Features: preStop graceful shutdown, socket-based probes, selfheal CronJob,
#           disk-pressure toleration, all persistent volumes
# Deploy: main3 (apexT1), main4 (apexT2), main5 (apexT3)
# ============================================================================
# Usage:
#   1. Edit POOL_NAME + hostPath base + hostPort per relay instance
#   2. kubectl apply -f apex-testnet-relay.yaml
#
# Per-relay customization:
#   apexT1 (main3): hostPort=3535, POOL_NAME=apexT1, paths=.../apexT1/
#   apexT2 (main4): hostPort=3536, POOL_NAME=apexT2, paths=.../apexT2/
#   apexT3 (main5): hostPort=3537, POOL_NAME=apexT3, paths=.../apexT3/
# ============================================================================
---
apiVersion: v1
kind: Namespace
metadata:
  name: relay-apexfusion-testnet
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: apext1-apexfusion-node
  namespace: relay-apexfusion-testnet
  labels:
    app: apext1-apexfusion-node
    network: afpt
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: apext1-apexfusion-node
  template:
    metadata:
      labels:
        app: apext1-apexfusion-node
        network: afpt
    spec:
      hostname: apext1
      imagePullSecrets:
        - name: ghcr-creds
      terminationGracePeriodSeconds: 300
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      tolerations:
        - key: node.kubernetes.io/disk-pressure
          operator: Exists
          effect: NoSchedule
      containers:
        - name: cardano-node
          image: ghcr.io/gvolcy/hybrid-node:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: NETWORK
              value: "afpt"
            - name: NODE_MODE
              value: "relay"
            - name: NODE_PORT
              value: "3535"
            - name: CNODE_PORT
              value: "3535"
            - name: POOL_NAME
              value: "apexT1"
            - name: UPDATE_CHECK
              value: "N"
            - name: CPU_CORES
              value: "2"
            - name: EKG_HOST
              value: "0.0.0.0"
            - name: PROMETHEUS_HOST
              value: "0.0.0.0"
            - name: PROMETHEUS_PORT
              value: "12798"
          ports:
            - containerPort: 3535
              hostPort: 3535
              name: node
              protocol: TCP
            - containerPort: 12798
              name: prometheus
              protocol: TCP
            - containerPort: 12788
              name: ekg
              protocol: TCP
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/bash
                  - -c
                  - |
                    killall -s SIGINT cardano-node 2>/dev/null || true
                    echo "Sent SIGINT, waiting for clean shutdown..."
                    for i in $(seq 1 280); do
                      [ -f /opt/cardano/cnode/db/clean ] && echo "Clean shutdown confirmed" && exit 0
                      sleep 1
                    done
                    echo "Timeout waiting for clean shutdown"
          startupProbe:
            exec:
              command: ["test", "-S", "/opt/cardano/cnode/sockets/node.socket"]
            initialDelaySeconds: 300
            periodSeconds: 30
            failureThreshold: 2880    # 24 hours grace for initial sync
          livenessProbe:
            exec:
              command: ["test", "-S", "/opt/cardano/cnode/sockets/node.socket"]
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 20
          readinessProbe:
            tcpSocket:
              port: node
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 10
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "4"
              memory: 4Gi
          volumeMounts:
            - mountPath: /opt/cardano/cnode/db
              name: db
            - mountPath: /opt/cardano/cnode/priv
              name: priv
            - mountPath: /opt/cardano/cnode/logs
              name: logs
            - mountPath: /opt/cardano/cnode/sockets
              name: sockets
            - mountPath: /opt/cardano/cnode/guild-db
              name: guild-db
            - mountPath: /opt/cardano/cnode/files
              name: files
      volumes:
        - name: db
          hostPath:
            path: /home/main2/blockchain-nodes/apexfusion/afpt-relay/apexT1/db
            type: DirectoryOrCreate
        - name: priv
          hostPath:
            path: /home/main2/blockchain-nodes/apexfusion/afpt-relay/apexT1/priv
            type: DirectoryOrCreate
        - name: logs
          hostPath:
            path: /home/main2/blockchain-nodes/apexfusion/afpt-relay/apexT1/logs
            type: DirectoryOrCreate
        - name: sockets
          hostPath:
            path: /home/main2/blockchain-nodes/apexfusion/afpt-relay/apexT1/sockets
            type: DirectoryOrCreate
        - name: guild-db
          hostPath:
            path: /home/main2/blockchain-nodes/apexfusion/afpt-relay/apexT1/guild-db
            type: DirectoryOrCreate
        - name: files
          hostPath:
            path: /home/main2/blockchain-nodes/apexfusion/afpt-relay/apexT1/files
            type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: apext1-apexfusion-service
  namespace: relay-apexfusion-testnet
spec:
  type: NodePort
  ports:
    - port: 3535
      targetPort: 3535
      name: node
    - port: 12798
      targetPort: 12798
      name: prometheus
  selector:
    app: apext1-apexfusion-node
---
# ============================================================================
# Selfheal CronJob: restarts relay if node stops responding
# ============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: apext1-selfheal
  namespace: relay-apexfusion-testnet
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: apext1-selfheal
  namespace: relay-apexfusion-testnet
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list"]
  - apiGroups: [""]
    resources: ["pods/exec"]
    verbs: ["create"]
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: apext1-selfheal
  namespace: relay-apexfusion-testnet
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: apext1-selfheal
subjects:
  - kind: ServiceAccount
    name: apext1-selfheal
    namespace: relay-apexfusion-testnet
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: apext1-selfheal
  namespace: relay-apexfusion-testnet
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 1
  successfulJobsHistoryLimit: 1
  jobTemplate:
    spec:
      activeDeadlineSeconds: 120
      template:
        spec:
          serviceAccountName: apext1-selfheal
          restartPolicy: OnFailure
          containers:
            - name: selfheal
              image: bitnami/kubectl:latest
              command:
                - /bin/bash
                - -c
                - |
                  DEPLOY="apext1-apexfusion-node"
                  NS="relay-apexfusion-testnet"
                  MIN_AGE=86400
                  PROM_PORT=12798
                  POD=$(kubectl get pods -n $NS -l app=$DEPLOY -o jsonpath='{.items[0].metadata.name}' 2>/dev/null)
                  if [ -z "$POD" ]; then
                    echo "No pod found, restarting deployment"
                    kubectl rollout restart deployment/$DEPLOY -n $NS
                    exit 0
                  fi
                  START=$(kubectl get pod -n $NS $POD -o jsonpath='{.status.startTime}' 2>/dev/null)
                  if [ -n "$START" ]; then
                    START_EPOCH=$(date -d "$START" +%s 2>/dev/null)
                    NOW_EPOCH=$(date +%s)
                    AGE=$(( NOW_EPOCH - START_EPOCH ))
                    if [ "$AGE" -lt "$MIN_AGE" ]; then
                      echo "Pod age ${AGE}s < MIN_AGE ${MIN_AGE}s, skipping (may still be syncing)"
                      exit 0
                    fi
                  fi
                  SYNC=$(kubectl exec -n $NS $POD -- curl -s localhost:${PROM_PORT}/metrics 2>/dev/null | grep 'cardano_node_metrics_density_real' | awk '{print $2}')
                  if [ -z "$SYNC" ] || [ "$SYNC" == "0" ]; then
                    echo "Node not responding or stuck (density=${SYNC:-null}), age=${AGE}s, restarting"
                    kubectl rollout restart deployment/$DEPLOY -n $NS
                  else
                    echo "Node healthy: density=$SYNC age=${AGE}s"
                  fi
